cd ${DATADIR}/bbc/reg/dwi_warps_in_template_space/comb_FA_AD_RD_MD_4vol
echo ${PWD}
rm -f LeftPostIntCap.*
list=`ls -d *LeftPost*`
for afile in $list
do
echo $afile
rm afile.*
cp $afile afile.nii.gz
fslchfiletype ANALYZE afile
${PYLABS}/pylabs/diffusion/channel_coreg
for m in fa ad rd md; do
    data=`cat ${m}.txt | tr -s " "`
    echo "${afile},${m}${data// /,}" >> LeftPostIntCap.csv
done
done
