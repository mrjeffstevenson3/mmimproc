list=`ls -d sub-bbc*`
rm /home/toddr/allvtk_run3.txt
for afolder in ${list}
do
echo working on ${afolder}
cd /mnt/users/js/bbc
cd ${afolder}/*/*/vtk_tensor_comp_run2
rm aal_motor.vtk
cp *aal_motor.vtk aal_motor.vtk
rm base.vtk
cp *base*.vtk base.vtk
list2=`ls *tensor*.vtk`
for afile in ${list2}
do
echo working on ${afile}
cp ${afile} f.vtk
/home/toddr/Software/pylabs/pylabs/diffusion/readfiber_binary_bbcb
echo ${afile}
echo ${afile} >> /home/toddr/allvtk_run3.txt
cat dti_results.txt >> /home/toddr/allvtk_run3.txt
done
done
